{% extends 'base.html.twig' %}

{% block stylesheets %}{% endblock %}

{% block javascripts %}{% endblock %}

{% block body %}
	<table class="table table-dark table-striped table-hover text-center table-bordered">
		<thead>
			<tr>
				<th scope="col">
					{% trans %}builds.name{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}builds.author{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}builds.weapon{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}builds.type{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}builds.view{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}builds.update{% endtrans %}
				</th>
			</tr>
		</thead>
		<tbody>
			{# {% for build in builds %}
							<tr>
								<td>
									<a href="{{path('app_builds_show',{: builds.id})}}">{{ builds.name }}</a>
								</td>
								<td>
									{{ builds.author.pseudo }}
								</td>
								<td>
									{{ builds.weapons|join(' / ') }}
								</td>
								<td>
									{% if builds.type == 0 %}//{% endif %}
									{% if builds.type == 1 %}PVP{% endif %}
									{% if builds.type == 2 %}PVE{% endif %}
									{% if builds.type == 3 %}
										PVPVE
									{% endif %}
									{% if builds.type == 4 %}Siège{% endif %}
								</td>
								<td>
									{{ builds.views }}
								</td>
								<td>
									{% set distance = date('now').diff(date(builds.UpdatedAt)).days %}il
									y a{% if distance <= 1 %}
										1 jour
									{% else %}
										{{ distance }}
										jours
									{% endif %}
								</td>
							</tr>
						{% endfor %} #}
		</tbody>
	</table>
{% endblock %}
