{% extends 'base.html.twig' %}

{% block stylesheets %}
	
{% endblock %}

{% block javascripts %}

{% endblock %}

{% block body %}
	<table class="table table-dark table-striped table-hover text-center table-bordered">
		<thead>
			<tr>
				<th scope="col">
					{% trans %}build.name{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}build.author{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}build.weapon{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}build.type{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}build.view{% endtrans %}
				</th>
				<th scope="col">
					{% trans %}build.update{% endtrans %}
				</th>
			</tr>
		</thead>
		<tbody>
			{# {% for build in builds %}
				<tr>
					<td>
						<a href="{{path('app_builds_show',{: build.id})}}">{{ build.name }}</a>
					</td>
					<td>
						{{ build.author.pseudo }}
					</td>
					<td>
						{{ build.weapons|join(' / ') }}
					</td>
					<td>
						{% if build.type == 0 %}//{% endif %}
						{% if build.type == 1 %}PVP{% endif %}
						{% if build.type == 2 %}PVE{% endif %}
						{% if build.type == 3 %}
							PVPVE
						{% endif %}
						{% if build.type == 4 %}Siège{% endif %}
					</td>
					<td>
						{{ build.views }}
					</td>
					<td>
						{% set distance = date('now').diff(date(build.UpdatedAt)).days %}il
						y a{% if distance <= 1 %}
							1 jour
						{% else %}
							{{ distance }}
							jours
						{% endif %}
					</td>
				</tr>
			{% endfor %} #}
		</tbody>
	</table>
{% endblock %}
